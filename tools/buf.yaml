version: v1beta1
build:
  roots:
    - .
lint:
  use:
    - BASIC
  except:
    - FILE_LOWER_SNAKE_CASE
    - ENUM_NO_ALLOW_ALIAS
    - IMPORT_NO_PUBLIC
    - PACKAGE_AFFINITY
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_SAME_DIRECTORY
breaking:
  use:
    - FILE
    - WIRE_JSON
